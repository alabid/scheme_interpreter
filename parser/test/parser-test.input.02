(define addOne
  (lambda (x)
    (+ x 1)))

;; map is a "higher order function"
;; it takes a function (and stuff) as arguments
;; map is a function
;; it takes a function as an argument, and applies that function
;; to each item in the list that follows
(map addOne '(5 2 12 3 4))


